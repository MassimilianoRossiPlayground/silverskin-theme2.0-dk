{%- assign collectionProdTitle = collection.title -%}
{%- assign collectionProdUrl = collection.url -%}

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
      "@type": "ListItem",
      "position": 1,
      "name": "{{ collectionProdTitle }}",
      "item": "{{ collectionProdUrl }}"
    }
  ]
  }
</script>